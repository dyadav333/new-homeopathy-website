import { redirect } from "next/navigation";
import { requireRole } from "@/lib/require-role";
import { DashboardNav } from "../DashboardNav";
export default async function SettingsPage() { const { authorized } = await requireRole(["PATIENT"]); if (!authorized) redirect("/login"); return <div className="dashboard-frame"><DashboardNav canSwitch={false} /><main className="dashboard-main"><div className="dashboard-heading"><div><p className="dashboard-kicker">Preferences</p><h1>Settings</h1><p>Account preferences will live here as notifications are connected.</p></div></div><div className="dashboard-card"><h2 className="font-display text-2xl text-brand-900">Notification preferences</h2><label className="mt-5 flex items-center gap-3 text-sm text-ink/70"><input type="checkbox" defaultChecked /> Appointment reminders by email</label><label className="mt-4 flex items-center gap-3 text-sm text-ink/70"><input type="checkbox" defaultChecked /> Important account updates</label></div></main></div>; }
"use client";
import { FormEvent, useState } from "react";
export function OrderForm() { const [message, setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const result = await fetch("/api/patient/orders", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ items: (form.elements.namedItem("items") as HTMLTextAreaElement).value }) }).then((response) => response.json()); setMessage(result.success ? "Order placed. It is now in your history." : result.error?.message ?? "Could not place order."); if (result.success) form.reset(); } return <form onSubmit={submit} className="profile-form"><label>What would you like to order?<textarea name="items" required minLength={3} rows={4} placeholder="List medicines recommended by your practitioner" /></label><div className="profile-actions"><span>{message}</span><button type="submit">Place order</button></div></form>; }
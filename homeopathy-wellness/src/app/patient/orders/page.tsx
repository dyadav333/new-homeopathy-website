import { redirect } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { requireRole } from "@/lib/require-role";
import { DashboardNav } from "../DashboardNav";
import { OrderForm } from "./OrderForm";
export default async function OrdersPage() { const { authorized, session } = await requireRole(["PATIENT"]); if (!authorized) redirect("/login"); const patient = await prisma.patient.findUnique({ where: { userId: session!.user.id }, include: { medicineOrders: { orderBy: { createdAt: "desc" } } } }); return <div className="dashboard-frame"><DashboardNav canSwitch={false} /><main className="dashboard-main"><div className="dashboard-heading"><div><p className="dashboard-kicker">Wellness shop</p><h1>Medicine orders</h1><p>Place a current order, then track its history here.</p></div></div><div className="dashboard-card"><OrderForm /></div><div className="dashboard-list">{patient?.medicineOrders.length ? patient.medicineOrders.map((order) => <div className="dashboard-list-item" key={order.id}><div><h3>Order #{order.id.slice(-6).toUpperCase()}</h3><p>{order.items}</p></div><span className="rounded-full bg-sky-100 px-3 py-1 text-xs font-semibold text-sky-800">{order.status}</span></div>) : <div className="dashboard-card"><h2 className="font-display text-2xl text-brand-900">No order history yet</h2><p className="mt-2 text-sm text-ink/60">Orders you place will appear here.</p></div>}</div></main></div>; }
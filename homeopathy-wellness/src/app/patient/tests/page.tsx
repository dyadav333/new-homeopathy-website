import { redirect } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { requireRole } from "@/lib/require-role";
import { DashboardNav } from "../DashboardNav";
export default async function TestsPage() { const { authorized, session } = await requireRole(["PATIENT"]); if (!authorized) redirect("/login"); const patient = await prisma.patient.findUnique({ where: { userId: session!.user.id }, include: { tests: { orderBy: { createdAt: "desc" } } } }); return <div className="dashboard-frame"><DashboardNav canSwitch={false} /><main className="dashboard-main"><div className="dashboard-heading"><div><p className="dashboard-kicker">Requested tests and reports</p><h1>My tests</h1><p>One view for doctor-requested tests, orders, and uploaded results.</p></div></div>{patient?.tests.length ? <div className="dashboard-list">{patient.tests.map((test) => <div className="dashboard-list-item" key={test.id}><div><h3>{test.name}</h3><p>{test.result ?? "No result uploaded yet"}</p></div><span className="rounded-full bg-amber-100 px-3 py-1 text-xs font-semibold text-amber-800">{test.status}</span></div>)}</div> : <div className="dashboard-card"><h2 className="font-display text-2xl text-brand-900">No tests yet</h2><p className="mt-2 text-sm text-ink/60">Tests requested by your practitioner and reports you upload will appear here.</p></div>}</main></div>; }